
// import { reducer as form } from 'redux-form';

const initial = {
  app: {}
};

export const  formReducer = (state = initial.app, { type, payload }) =>  {
  switch (type) {
  }
  return state;
}

// export default combineReducers(
//   { app, form }
// );
